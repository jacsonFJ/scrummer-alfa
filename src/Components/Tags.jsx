import { styled } from "styled-components";
import Colors from "../Colors";

export const TagBlue = styled.span`
  display: flex;
  align-items: center;
  padding: 10px;
  border-radius: 18px;
  background: ${Colors.neutral50};
  color: ${Colors.neutral500};
  font-size: 12px;
  font-weight: 400;
`;
